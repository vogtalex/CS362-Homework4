def createFullName(firstName, lastName):
	space = ' '
	fullname = firstName + space + lastName
	return (fullname)

print('\nThis program will output a full name when given a first and last name')